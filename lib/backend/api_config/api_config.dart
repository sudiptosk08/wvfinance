class ApiConfig {
  /// change the mainDomain to your api domain and apiVersion to the current api version
  static const String mainDomain = "https://wvfinance.org";
  static const String apiVersion = "api";
  static const String baseUrl = "$mainDomain/$apiVersion";
}
